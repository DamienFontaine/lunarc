language: go
sudo: false
go:
  - 1.6
services:
  - mongodb
before_install:
  - go get github.com/axw/gocov/gocov
  - go get github.com/modocache/gover
  - go get github.com/mattn/goveralls
  - if ! go get github.com/golang/tools/cmd/cover; then go get golang.org/x/tools/cmd/cover; fi
before_script:
  - mongorestore testdata/mongo
script:
  - go test -coverprofile=config.coverprofile ./config -v
  - go test -coverprofile=controllers.coverprofile ./controllers -v
  - go test -tags=integration -coverprofile=datasource.coverprofile ./datasource -v
  - go test -coverprofile=models.coverprofile ./models -v
  - go test -tags=integration -coverprofile=services.coverprofile ./services -v
  - go test -coverprofile=utils.coverprofile ./utils -v
  - go test -coverprofile=core.coverprofile -v 
  - $HOME/gopath/bin/gover
  - $HOME/gopath/bin/goveralls -coverprofile=gover.coverprofile -service=travis-ci
